{% extends "base.html.twig" %}

{%  block title %}
    Détail d'un évènement
{% endblock %}

{%  block body %}
    <a href="{{ path('App\\Controller\\EventController::detail_id', {'id': '1'}) }}"><i class="fas fa-arrow-alt-circle-left fs-2 mb-4"></i></a>
    <div class="row g-3">
        <div class="col-lg-4 blue txt_desktop">
            {%  include 'partials/_event_card.html.twig' %}
        </div>
        <div class="col-lg-4 green">
            {%  include 'partials/_event_card.html.twig' %}
        </div>
        <div class="col-lg-4 brown txt_desktop">
            {%  include 'partials/_event_card.html.twig' %}
        </div>
    </div>
    <div class="text-end">
        <a href="{{ path('App\\Controller\\EventController::detail_id', {'id': '1'}) }}"><i class="fas fa-arrow-alt-circle-right fs-2 "></i></a>
    </div>
{%  endblock %}
