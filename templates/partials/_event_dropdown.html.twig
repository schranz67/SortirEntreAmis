<select name="{{ name|default('items') }}" class="form-select mb-3 dd_w" onchange="this.form.submit()">
    <option value="0">Tout</option>
    {% for item in items %}
        <option value={{ item.id }} {% if criterion == item.id %}selected{% endif %}>{{ item.name }}</option>
    {% endfor %}
</select>
